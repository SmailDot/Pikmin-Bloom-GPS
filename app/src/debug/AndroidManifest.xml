<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug builds only: lets adb drive the patrol for automated testing
             (see DebugCommandReceiver for the broadcast syntax). -->
        <receiver
            android:name=".debug.DebugCommandReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="app.pikminbloom.gps.DEBUG_CMD" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
